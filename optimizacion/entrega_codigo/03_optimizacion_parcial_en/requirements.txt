# Versiones EXACTAS con las que se generaron los resultados reportados en el
# articulo COMROB. Reproducen bit a bit las metricas de error, fitness, nfev,
# los hiperparametros elegidos por Optuna y los 16 parametros optimizados
# (ED y GA), verificado con semilla 42.
#
# IMPORTANTE: Optuna y SciPy determinan los resultados finales.
#   - Optuna (muestreador TPE) elige los hiperparametros; una version distinta
#     selecciona hiperparametros distintos para la MISMA semilla y, por tanto,
#     produce una corrida profunda distinta.
#   - SciPy fija la trayectoria de differential_evolution.
# Cambiar estas versiones puede alterar los numeros reportados aunque la
# semilla sea la misma. Instala con:  pip install -r requirements.txt
#
# Probado con Python 3.9 y 3.11.
numpy==2.0.2
scipy==1.13.1
pandas==2.3.3
matplotlib==3.9.4
optuna==4.9.0
